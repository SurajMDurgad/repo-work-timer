{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Repo Work Timer",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/src/**/*.js"]
    }
  ]
}
